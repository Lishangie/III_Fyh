model:
  name: "credit_risk_scoring"
  version: "1.0.0"
  default: gradient_boosting
  random_state: 42
  test_size: 0.15
  val_size: 0.15

service:
  host: "0.0.0.0"
  port: 8000
  log_level: "INFO"

risk_thresholds:
  low: 0.3
  high: 0.6
